// What is the dart that he starts with?
DartGun.DartType = 1 // 1 --> Poison; 2--> Muteness; 3--> Blindness (default: 1)

// BaseDamage taken by the target when hit by any dart
DartGun.BaseDamage = 3 // (default: 3)

// The damage the target takes from Poison is a random number from "PoisonMIN" to "PoisonMAX" every "PoisonEffectSeconds"
DartGun.PoisonMIN = 3 // (default: 3)

DartGun.PoisonMAX = 6 // (default: 6)

DartGun.PoisonEffectSeconds = 1.75 // (default: 1.75)

// How many times does the Target takes damage from poison
DartGun.PoisonTakeDmgRepetitions = 13 // (default: 13)

// For how long does the Target stays muted with Muteness dart (in seconds)
DartGun.MuteTime = 5 // (default: 5)

// For how long does the Target stays blind with Blindness dart (in seconds)
DartGun.BlindTime = 4 // (default: 4)